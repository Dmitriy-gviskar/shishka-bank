# Беклог чата

## ✅ Стикеры
Сетка лесных эмодзи 🦊🐿️🦉🐻🦌🐰🌲🍄🌰🍂🌟❤️🔥😂👍🎉 в чате.
Отправляются как `type: 'sticker'`, отображаются крупно без пузыря.

## ⏳ Статус «в лесу»
Зелёная точка на аватарке — человек недавно был онлайн.
Нужно: поле `last_seen` в users, обновлять при каждом запросе, отдавать в `/api/chat/list`.

## ⏳ Реакции
Двойной тап / долгое нажатие на сообщение → ❤️🔥😂.
Нужно: таблица `message_reactions`, endpoint'ы `/api/message/react` и `/api/message/reactions`.

## ⏳ Push-уведомления
Уведомление о новом сообщении, даже если приложение закрыто.
WebPush уже настроен (`server-pg.mjs:sendPush`), нужно дёргать при `/api/message`.

## ⏳ Голосовые сообщения
«Шёпот леса» — запись голоса.
Схема `type: 'audio'` и `send_whisper` уже есть в БД.
Нужно: MediaRecorder API на клиенте, загрузка аудио на сервер.

## ⏳ Ответ на сообщение
Свайп вправо → процитировать сообщение перед ответом.
Нужно: поле `reply_to` в messages, UI для цитаты.

## ⏳ Стикер-подарок
Отправить стикер + 1 шишку вместе.
Нужно: совместить `/api/message` + `/api/transfer` в одном запросе.

## ⏳ Лесной почтальон
Достижение за N отправленных сообщений.
Уже есть `check_achievements` в `send_message` — нужно добавить achievement в каталог.
